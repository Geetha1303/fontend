pipeline {
  agent {
    label 'NODEJS'
    }

  stages {

    stage('prepare Artifacts') {
      steps {
        sh '''
          cd static
          zip -r ../frontend.zip *
        '''
      }
    }
  }
 }